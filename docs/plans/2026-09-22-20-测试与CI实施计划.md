# DebateTimer 测试与 CI 实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 给 DebateTimer 建四道自动化闸门——代码格式检查、单元测试、e2e 测试、编译检查——其中 e2e 与编译检查在 Windows（10 世代 / 11 世代 / 真 Win11 arm64）、Ubuntu Linux、macOS 上各跑一遍。

**Architecture:** 两条流水线。`quality` 是 Linux 单机快速道（格式 + 类型 + 单测），PR 上几十秒出结论；`platform` 是平台矩阵道（编译检查 + 真实桌面 e2e），三平台阻塞、两个探测行非阻塞。e2e 用 WebdriverIO 的 **embedded provider**（WebDriver 服务跑在应用进程内），因此 Windows / Linux / macOS 三平台同一套配置、无外部 driver、无付费 key。

**Tech Stack:** Tauri 2.11 · Svelte 5.56（runes）· SvelteKit 2.65 + adapter-static（SPA）· TypeScript 6.0 · Vite 8.0 · Tailwind 4.3 · Vitest 3.2 · Prettier 3.9 + prettier-plugin-svelte 4.1 + prettier-plugin-tailwindcss 0.8 · `@wdio/tauri-service` 1.4 + `tauri-plugin-wdio-webdriver` 1.4 · rustfmt/clippy · npm（**pnpm 本机不可用**）· GitHub Actions

---

## 0. 决策记录（2026-09-22 已拍板）

| 决策点       | 结论                                                                                               | 理由                                                                                         |
| ------------ | -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| CI 平台      | **GitHub Actions**                                                                                 | 仓库托管在 GitHub（当前 remote 是占位符 `github.com/not-exist/DebateTimer`，需换成真实地址） |
| Windows 覆盖 | **`windows-2022`（Win10 世代）+ `windows-2025`（Win11 世代）+ `windows-11-arm`（真 Win11 arm64）** | 见 §2 的平台映射说明                                                                         |
| e2e 层级     | **Tauri 真实桌面 e2e**（驱动真实应用窗口），不做纯 Web 层替身                                      | 能测到 Tauri IPC、原生窗口、真实键盘事件                                                     |
| 格式检查边界 | **Prettier + `cargo fmt` + `cargo clippy -D warnings`**，暂不引入 ESLint                           | Svelte/TS 的语义检查由已有的 `svelte-check`（`npm run check`）承担                           |
| e2e 门禁时机 | **每个 PR 就跑三平台全量 e2e**                                                                     | 用户明确要求；代价见 §4.3 的成本说明                                                         |
| 包管理       | npm（`npm ci`，用 `package-lock.json`）                                                            | pnpm 本机因 corepack 报错不可用                                                              |

---

## 1. 与既有项目约束的衔接

- **三平台完全同等**（`memory: project_debatetimer`）：所以三平台都必须跑 e2e，不能只跑 Windows。
- **全屏应用必须有退出入口**（`memory: feedback_quit_entry`）：退出路径必须被 e2e 覆盖，这是已验收过的功能点，不能回归。
- **GPL-3.0 文本必须逐字保留**（commit `5923f0d`）：`.prettierignore` 必须排除 `LICENSE`，否则格式化会破坏 license 检测。
- **教室无网 / 字体本地打包**：CI 里不做任何 CDN 依赖，与前缀一致。

---

## 2. 平台映射：为什么是这三个 Windows runner

这是本计划里唯一需要"如实告知缺口"的地方，先讲清楚再动手。

**硬约束：GitHub 托管 runner 不存在真 Windows 10 / Windows 11 的 x64 镜像。** 已核实的可用集合是 `windows-2022`（=Windows Server 2022，内核 10.0.20348）、`windows-2025` / `windows-latest`（=Windows Server 2025，内核 10.0.26100）、`windows-11-arm` / `windows-11-vs2026-arm`（字面 Windows 11，但 arm64）。来源：[GitHub-hosted runners reference](https://docs.github.com/en/actions/reference/runners/github-hosted-runners)、[actions/runner-images README](https://github.com/actions/runner-images)。

**已排查过的社区替代方案（均不采用，理由如下）**

- **其他托管 CI**：Azure Pipelines / AppVeyor / CircleCI / Cirrus CI / Buildkite / TeamCity Cloud 全部只提供 Server 镜像；SourceHut 连 Windows 都没有。GitHub larger runner 的自定义镜像也只能基于 GitHub 自有 Server 基底，不能自带 Win10/11 ISO。→ 无解。
- **`dockur/windows`（容器里跑真 Win10/11 VM）**：需要宿主暴露 `/dev/kvm`。GitHub 官方对标准 Linux runner 的立场是"技术上可行但不受支持、实验性、无 SLA"（[runner-images#7670](https://github.com/actions/runner-images/issues/7670)），且 `ubuntu-26.04-arm` 上已确认缺失（[#14549](https://github.com/actions/runner-images/issues/14549)）。该项目的 README 明确要求 `--device=/dev/kvm`，并且**找不到任何一份已公开跑通的 GitHub Actions workflow**。冷启动需完整安装 5–8GB ISO，量级 15–40 分钟。此外 Win11 客户端虚拟化在许可上需要 VDA / Enterprise（[微软授权指引](https://www.microsoft.com/licensing/guidance/Windows-11-Licensing-for-Virtual-Desktops)）。→ 判定为实验级，不进主流水线。
- **自建 runner 挂真 Win10 机器**：字面满足度最高，但官方**没有**"可选 / 非阻塞 job"机制——job 排队时若 runner 离线，会**挂满 24 小时才被自动取消**（[limits 文档](https://docs.github.com/en/actions/reference/limits)）。→ 本次不采用，但保留为后续可选扩展（见 §7）。

**采用的映射（诚实标注）**

| runner           | 对应真实环境                 | 在矩阵里的角色                               |
| ---------------- | ---------------------------- | -------------------------------------------- |
| `windows-2022`   | Windows 10 世代内核 + WOW64  | **主 Windows e2e**，阻塞 PR                  |
| `windows-2025`   | Windows 11 世代内核          | 编译检查阻塞；e2e 作为**非阻塞探测**（见下） |
| `windows-11-arm` | **字面 Windows 11**（arm64） | 非阻塞探测，实验性                           |

> ⚠️ **必须写进 README 的缺口**：以上三个都不是普通用户的 Windows 10 / 11 消费版桌面。测不到的东西包括：消费级 WebView2 版本差异、DPI 缩放、Windows Defender / SmartScreen 拦截、非管理员权限运行。这些仍须保留一份**手工验收清单**。

**关于 `windows-2025` 上到底能不能跑 e2e：两条冲突证据**

- [actions/runner-images#14738](https://github.com/actions/runner-images/issues/14738)（2026-09-16 提交，**仍 open**）：`tauri-driver` + `msedgedriver` 在 `windows-2025-vs2026` 上确定性失败 `DevToolsActivePort file doesn't exist`，同 commit 在 `windows-2022` 上 8/8 通过。
- [webdriverio/desktop-mobile#542](https://github.com/webdriverio/desktop-mobile/issues/542)：根因更普适——GitHub runner **以管理员身份运行**，Edge 150+（2026-07 起）在该条件下丢失 `--remote-debugging-port`，wry 的上游修复被拒。受影响的是经 CDP 的 `official` / `crabnebula` provider；**embedded provider 不走 CDP，免疫此问题**。

结论：既然我们选 embedded provider，#14738 未必适用于我们。但"未必"不是"确定"，所以 `windows-2025` 上的 e2e 先做成**非阻塞探测**收集证据，跑绿若干次后再提升为阻塞（§5 Task 12）。

> 同一发现也意味着：**Tauri 官方那份 [WebDriver CI 文档](https://v2.tauri.app/develop/tests/webdriver/ci/) 已经过时**——它教的是 `tauri-driver` + `msedgedriver`，在所有 GitHub Windows runner 上都已失效，而且完全没覆盖 macOS。不要照抄那份 workflow。

---

## 3. 四道闸门定义

| 闸门           | 命令                                                                                      | 跑在哪                                 | 说明                                                               |
| -------------- | ----------------------------------------------------------------------------------------- | -------------------------------------- | ------------------------------------------------------------------ |
| ① 代码格式检查 | `npm run format:check` + `cargo fmt --check` + `cargo clippy -D warnings`                 | Linux 单机                             | clippy 归在这一道（它是"代码规范"而非"能编译"）                    |
| ② 单元测试     | `npm test`（vitest run）+ `cargo test`                                                    | Linux 单机                             | 当前 vitest 31 个用例全绿（已实测）；Rust 侧目前零测试，命令先接上 |
| ③ e2e          | `npm run test:e2e`                                                                        | ubuntu-24.04 / windows-2022 / macos-15 | WebdriverIO + embedded provider                                    |
| ④ 编译检查     | `npm run check`（svelte-check）+ `npm run build`（vite）+ `npm run tauri build`（真打包） | 五平台矩阵                             | 逐平台真打包才算"平台编译通过"                                     |

### 3.1 ⚠️ `frontendDist` 缺失会 panic —— 但被 feature 门住（2026-09-22 实测更正）

> 本节原来的判断是"`cargo check` / `cargo clippy` / `cargo test` 三者都必须在 `npm run build` 之后运行"。
> **Task 6 的实测推翻了这一条**，以下是更正后的结论。

`src-tauri/tauri.conf.json` 里 `frontendDist: "../build"`，`tauri-codegen` 展开 `tauri::generate_context!()` 时确实会检查该目录是否存在，不存在就 panic：

```rust
// tauri-codegen 2.6.3 src/context.rs:187-190
if !assets_path.exists() {
    panic!("The `frontendDist` configuration is set to `{path:?}` but this path doesn't exist")
}
```

**但这个检查只在"非 dev"编译路径上才会走到。** 同一文件 `src/context.rs:178`：

```rust
} else if dev && config.build.dev_url.is_some() {
    // 用空 EmbeddedAssets：前端产物一个都不嵌入，frontendDist 的存在性也不检查
```

而 `dev` 不由 profile 决定，由 **feature** 决定（`tauri-macros 2.6.3 src/context.rs:155`）：

```rust
dev: cfg!(not(feature = "custom-protocol")),
```

本项目 `src-tauri/tauri.conf.json:8` 已配置 `"devUrl": "http://localhost:1420"`，于是：

| 命令                                           | 是否启用 `tauri/custom-protocol` | `build/` 缺失时                   |
| ---------------------------------------------- | -------------------------------- | --------------------------------- |
| `cargo check` / `clippy` / `test`              | 否 → `dev = true`                | **通过**（走 178 行分支跳过检查） |
| `cargo check --features tauri/custom-protocol` | 是 → `dev = false`               | **panic**（exit 101）             |

**实测记录（2026-09-22，本机 Linux，Tauri 2.11.6）**

```bash
mv build /tmp/dbt-build-backup
npm run check:rust     # EXIT=0  ← 原预期为失败
npm run lint:rust      # EXIT=0
npm run test:rust      # EXIT=0, test result: ok. 0 passed
cargo clean -p tauri-app && npm run check:rust   # 仍 EXIT=0（Removed 743 files / 3.3GiB），排除增量缓存干扰
cargo check --features tauri/custom-protocol     # EXIT=101，复现出 frontendDist panic
```

**结论**

- `cargo check` / `clippy` / `test` **不需要** `npm run build` 前置。`quality.yml` 不会因为全新 clone 缺 `build/` 而炸。
- panic 仍然真实存在，只在启用 `tauri/custom-protocol` 的构建里触发——即 `tauri build`。而 `tauri build` 会先执行 `beforeBuildCommand: npm run build`，顺序自动满足。
- 所以「build 必须先于 Rust 命令」**不是承重约束**。`verify` 脚本继续保留，但它的价值是"一条命令跑完全部闸门"的便利聚合，**不是**规避 frontendDist panic。
- ⚠️ 若日后把 `devUrl` 从 `tauri.conf.json` 移除，178 行的分支不再命中，本节结论立即失效——那时 `cargo check` 会在缺 `build/` 时 panic。

---

## 4. 目标文件结构

```
.github/workflows/
  quality.yml            # 新建：Linux 快速道（格式 + 类型 + 单测）
  platform.yml           # 新建：平台矩阵（编译检查 + e2e）
.prettierrc.json         # 新建
.prettierignore          # 新建
rust-toolchain.toml      # 新建：把本机 Rust 1.98 钉死，避免 CI 漂移
e2e/
  wdio.conf.ts           # 新建
  specs/
    smoke.e2e.ts         # 新建：应用能起来
    timing.e2e.ts        # 新建：计时、切环节
    hotkeys.e2e.ts       # 新建：输入态屏蔽单键（高价值守卫）
    quit.e2e.ts          # 新建：退出入口 + 二次确认
package.json             # 修改：加 scripts 与 devDependencies
src-tauri/Cargo.toml     # 修改：加 wdio webdriver 插件（仅 debug）
src-tauri/src/lib.rs     # 修改：注册插件（仅 debug）
src-tauri/capabilities/default.json   # 修改：加 wdio-webdriver 权限
README.md                # 修改：补测试与平台覆盖缺口说明
```

---

## 5. 任务

### M1 · 本机基础设施

---

#### Task 1: 钉住 Rust 工具链

**Files:** Create `rust-toolchain.toml`

**Step 1:** 确认本机版本（已实测：`rustfmt 1.9.0-stable`、`clippy 0.1.98`、`cargo 1.98.0`）

```bash
rustc -V && cargo fmt --version && cargo clippy --version
```

**Step 2:** 创建 `rust-toolchain.toml`：

```toml
[toolchain]
channel = "1.98.0"
components = ["rustfmt", "clippy"]
```

**Step 3:** 验证 rustup 认账：

```bash
rustup show active-toolchain
```

Expected: 输出包含 `1.98.0`。

**Step 4:** 提交

```bash
git add rust-toolchain.toml && git commit -m "chore: pin rust toolchain to 1.98.0 with rustfmt and clippy"
```

---

#### Task 2: 接入 Prettier，先量出改动规模

**Files:** Create `.prettierrc.json`, `.prettierignore`; Modify `package.json`

**Step 1:** 安装（版本已核实：`prettier` 3.9.8 / `prettier-plugin-svelte` 4.1.1 / `prettier-plugin-tailwindcss` 0.8.1）

```bash
npm install -D prettier prettier-plugin-svelte prettier-plugin-tailwindcss
```

> `prettier-plugin-svelte` v4 **只**支持 Prettier 3 与 Svelte 5+，与本项目匹配（[插件 repo](https://github.com/sveltejs/prettier-plugin-svelte)）。`prettier-plugin-tailwindcss` **没有 v4**，最新就是 0.8.x，它通过 `tailwindStylesheet` 支持 Tailwind 4 的 CSS-first 配置（[插件 repo](https://github.com/tailwindlabs/prettier-plugin-tailwindcss)）。

**Step 2:** 创建 `.prettierrc.json`。**插件顺序有硬要求：`prettier-plugin-tailwindcss` 必须放最后**（该插件 README 明确要求，并显式列出与 `prettier-plugin-svelte` 的兼容）：

```json
{
  "plugins": ["prettier-plugin-svelte", "prettier-plugin-tailwindcss"],
  "overrides": [{ "files": "*.svelte", "options": { "parser": "svelte" } }],
  "tailwindStylesheet": "./src/app.css",
  "printWidth": 100,
  "semi": true,
  "singleQuote": false,
  "trailingComma": "all"
}
```

**Step 3:** 创建 `.prettierignore`：

```
node_modules
build
.svelte-kit
package-lock.json
src-tauri/target
src-tauri/gen
src-tauri/Cargo.lock

# 许可证文本必须逐字保留（见 commit 5923f0d：GPL-3.0 原文完整性用于 license 检测）
LICENSE
static/fonts/OFL.txt
```

**Step 4: 先量后改——不要盲跑 `--write`**

```bash
npx prettier --check .
```

Expected: 大概率报一批文件不合格。**记录文件数量**，这决定下一步怎么提交。

**Step 5: 决策点（必须让用户拍板，不要自己决定）**

- 若不合格文件 **≤ 5 个**：直接 `npx prettier --write .`，与配置合并成一个 commit。
- 若不合格文件 **> 5 个**：**单独一个纯格式化 commit**（`git commit -m "style: apply prettier to existing sources"`，body 里写明"本 commit 无逻辑改动"），把配置 commit 与格式化 commit 分开。这样日后 `git blame` / `git bisect` 不被淹掉，也方便用户 review。

**Step 6:** 加脚本到 `package.json`：

```json
"format": "prettier --write .",
"format:check": "prettier --check ."
```

**Step 7:** 验证：

```bash
npm run format:check
```

Expected: `All matched files use Prettier code style!`

**Step 8:** 提交

```bash
git add .prettierrc.json .prettierignore package.json package-lock.json
git commit -m "chore: add prettier for svelte 5 and tailwind 4"
```

---

#### Task 3: 补齐 npm scripts

**Files:** Modify `package.json`

**Step 1:** 在 `scripts` 里加（保留已有的 `dev` / `build` / `check` / `test` / `tauri`）：

```json
"format": "prettier --write .",
"format:check": "prettier --check .",
"fmt:rust": "cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check",
"lint:rust": "cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings",
"check:rust": "cargo check --manifest-path src-tauri/Cargo.toml --all-targets",
"test:rust": "cargo test --manifest-path src-tauri/Cargo.toml",
"test:e2e:build": "tauri build -- --debug --no-bundle",
"test:e2e": "wdio run e2e/wdio.conf.ts"
```

> 用 `--manifest-path` 而不是 `cd src-tauri`，这样从仓库根调用时 `frontendDist` 的相对解析仍正确（它相对 `tauri.conf.json` 所在目录解析）。避免 `cd` 让 `format:check` 与 Rust 检查混在同一个 shell 里时路径错乱。

**Step 2:** 验证 `format:check` 与 `lint:rust` 都能跑（此时 `build/` 存在，所以 clippy 应该能过）：

```bash
npm run format:check && npm run lint:rust
```

Expected: 两个都通过。若 clippy 报 warning，先修（数量应该很少，代码目前很干净）。

**Step 3:** 提交

```bash
git add package.json
git commit -m "chore: add format, rust lint and e2e npm scripts"
```

---

#### Task 4: 单元测试现状确认

**Files:** 无

**Step 1:** 跑前端单测（已实测基线：4 文件 31 用例全绿，耗时 ~1s）

```bash
npm test
```

Expected: `Test Files  4 passed (4)` / `Tests  31 passed (31)`。

**Step 2:** 跑 Rust 单测（当前断言为"0 个测试但编译通过"）：

```bash
npm run build && npm run test:rust
```

Expected: `test result: ok. 0 passed`。**实测通过**（本机：`build` 后 `test:rust` 三个 target 全部 `0 passed; 0 failed`）。
原先这里写的"必须先 build"已被 Task 6 证伪——见 §3.1。

**Step 3:** 不做代码改动，本 Task 只建立基线。若 Rust 侧有编译错误，就地排查（缺 `build/` **不会**是原因，见 §3.1）。

---

#### Task 5: 登记 Rust 零测试的现状

**Files:** Modify `docs/plans/2026-09-22-20-测试与CI实施计划.md`（本文件，勾掉即可）

Rust 侧目前没有任何测试，且 `lib.rs` 只做插件注册、没有业务逻辑——**不强行造测试**（YAGNI）。但要保证 `cargo test` 这条闸门已经在 CI 里接通，将来加原生逻辑时自动生效。本 Task 无需改动代码，仅在 PR 描述里说明。

---

#### Task 6: 实测 `frontendDist` panic 的真实触发条件（已完成，结论见 §3.1）

**Files:** Modify `package.json`

**Step 1:** 加一个聚合脚本，一条命令跑完全部闸门：

```json
"verify": "npm run build && npm run format:check && npm run check && npm test && npm run lint:rust && npm run test:rust"
```

> 脚本保留 `npm run build` 打头是为了让这条命令自包含（先有产物再查依赖产物的东西），
> **不是**因为 Rust 命令要求 `build/` 存在——那一条已被下面 Step 2 的实测推翻。

**Step 2: 实测这条约束是否真实存在**（这是本 Task 的核心，不要跳过）：

```bash
mv build /tmp/dbt-build-backup
npm run check:rust
```

**实测结果（2026-09-22）：没有失败，EXIT=0。** 原因见 §3.1：`tauri.conf.json` 配了 `devUrl`，
且 `cargo check` 不启用 `custom-protocol`，于是 `tauri-codegen` 走了 `dev && dev_url.is_some()` 分支，
整个 frontendDist 存在性检查被跳过。`cargo clean -p tauri-app` 后重跑仍为 EXIT=0，排除增量缓存干扰。

追加验证：`cargo check --features tauri/custom-protocol` → EXIT=101，**panic 成功复现**，
证明 panic 仍在，只是被 feature 门住。

**Step 3:** 恢复：

```bash
mv /tmp/dbt-build-backup build
npm run check:rust
```

**实测结果：通过（EXIT=0）；`lint:rust` / `test:rust` 亦复测通过。**

**Step 4:** 提交

```bash
git add package.json
git commit -m "chore: add aggregate verify npm script"
```

> 提交信息刻意不写 "documenting build-before-rust ordering"——该因果已不成立，
> 写进 git log 会长期误导后来人。

---

### M2 · e2e 地基

---

#### Task 7: 加 embedded WebDriver 插件（含 release 构建不被破坏的验证）

这一步是全计划**风险最高**的地方，所以拆成先验证再加码。

**Files:** Modify `src-tauri/Cargo.toml`, `src-tauri/src/lib.rs`, `src-tauri/capabilities/default.json`

**Step 1:** 按 WebdriverIO 官方文档改 `src-tauri/Cargo.toml`（[plugin-setup](https://webdriver.io/docs/desktop-testing/tauri/plugin-setup) 明确给的是 target-cfg 门控依赖，而不是普通依赖）：

```toml
[target.'cfg(debug_assertions)'.dependencies]
tauri-plugin-wdio-webdriver = "1"
```

**Step 2:** 改 `src-tauri/src/lib.rs`：

```rust
#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    let builder = tauri::Builder::default().plugin(tauri_plugin_opener::init());

    // 测试专用：在应用进程内起一个 W3C WebDriver HTTP server。
    // 只在 debug 构建存在——release 包绝不能带可被远程驱动的服务。
    #[cfg(debug_assertions)]
    let builder = builder.plugin(tauri_plugin_wdio_webdriver::init());

    builder
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

**Step 3:** 改 `src-tauri/capabilities/default.json`，加 `wdio-webdriver:default`：

```json
{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default", "wdio-webdriver:default"]
}
```

**Step 4: 关键验证——release 构建会不会被破坏**

这里有个文档没回答的问题：`[target.'cfg(debug_assertions)'.dependencies]` 会在 release 里**移除该 crate**，但 capability 仍然引用 `wdio-webdriver:default` 权限。Tauri 的 ACL 是从"已编译进来的插件"里解析权限的，所以 release 构建**有可能会因为"权限找不到"而失败**。

```bash
npm run build
npm run tauri build
```

Expected（理想）：构建成功。
若失败且报 `permission wdio-webdriver:default not found` 之类：**改用下面的兜底方案 B**，并回到本文件记录该行为。

**兜底方案 B**（release 能过，代价是 release 二进制里多链接一段永不启用的代码——`.plugin()` 不被调用就不会起 HTTP server）：

```toml
[dependencies]
tauri-plugin-wdio-webdriver = "1"
```

`lib.rs` 里的 `#[cfg(debug_assertions)]` 保持不变。

**Step 5: 验证 release 二进制确实不带可用的 WebDriver 服务**

```bash
# 抓 release 二进制路径（macOS/Linux 无扩展名，Windows 为 .exe）
ls src-tauri/target/release/ | grep -i tauri
# 手动起来，确认 4445 端口没人监听
./src-tauri/target/release/tauri-app & sleep 5
curl -s --max-time 2 http://127.0.0.1:4445/status || echo "OK: 无 WebDriver 服务"
kill %1
```

Expected: 输出 `OK: 无 WebDriver 服务`。**若这里能连上，立即停下来排查**——等于发布包里带了后门。

**Step 6:** 提交

```bash
git add src-tauri/Cargo.toml src-tauri/Cargo.lock src-tauri/src/lib.rs src-tauri/capabilities/default.json
git commit -m "test: add embedded webdriver plugin for debug builds only"
```

---

#### Task 8: 最小 e2e 跑通（先证明工具链，不写业务断言）

**Files:** Create `e2e/wdio.conf.ts`, `e2e/specs/smoke.e2e.ts`; Modify `package.json`

**Step 1:** 安装（版本已核实：`@wdio/tauri-service` 1.4.0 / `webdriverio` 9.32.0）

```bash
npm install -D @wdio/cli @wdio/local-runner @wdio/mocha-framework @wdio/spec-reporter @wdio/tauri-service tsx
```

> TS 配置需要 `tsx`（wdio 用它编译配置）。`tsx` **不做类型检查**——真正的类型检查由 `npm run check` 负责，这是有意的分工。

**Step 2:** 创建 `e2e/wdio.conf.ts`。**二进制路径已实测确认**：Cargo package name 是 `tauri-app`（不是 `productName` 辩论计时器），本机产物为 `src-tauri/target/debug/tauri-app`。

```ts
import { spawnSync } from "node:child_process";
import { existsSync } from "node:fs";
import process from "node:process";

const isWindows = process.platform === "win32";
const binary = `./src-tauri/target/debug/tauri-app${isWindows ? ".exe" : ""}`;

export const config: WebdriverIO.Config = {
  runner: "local",
  specs: ["./e2e/specs/**/*.e2e.ts"],
  maxInstances: 1,
  framework: "mocha",
  reporters: ["spec"],
  mochaOpts: { ui: "bdd", timeout: 120_000 },

  // 二进制不存在（或需要刷新）时先构建。CI 上由 workflow 显式构建，本地靠这里兜底。
  onPrepare: () => {
    if (!existsSync(binary) || process.env.DBT_REBUILD_E2E === "1") {
      const r = spawnSync("npm", ["run", "test:e2e:build"], {
        stdio: "inherit",
        shell: true,
      });
      if (r.status !== 0) throw new Error("e2e 目标构建失败");
    }
  },

  services: [
    [
      "tauri",
      {
        appBinaryPath: binary,
        driverProvider: "embedded",
      },
    ],
  ],
  capabilities: [{ browserName: "tauri" }],
};
```

> `services: [['tauri', ...]]` 与 `'@wdio/tauri-service'` 两种写法官方都接受。embedded provider 在 macOS 上会自动检测；Windows/Linux 上由 service 通过 `TAURI_WEBDRIVER_PORT` 通知应用。
> **不做** `withGlobalTauri: true`——那会在 release 里暴露 `window.__TAURI__`。只有在 Step 5 确认缺它就跑不起来时才加，并同步在 §7 记录。

**Step 3:** 写最小 smoke，先只验证"进程能起、能建 session、能读 DOM"：

```ts
// e2e/specs/smoke.e2e.ts
describe("冒烟", () => {
  it("应用能启动并渲染出标题", async () => {
    await expect($("main")).toBeDisplayed();
  });

  it("窗口标题是「辩论计时器」", async () => {
    await expect(browser).toHaveTitle("辩论计时器");
  });
});
```

**Step 4:** 本机跑（Linux，headless 需要虚拟显示）：

```bash
sudo apt-get install -y webkit2gtk-driver xvfb   # 若未装
npm run test:e2e:build
xvfb-run -a npm run test:e2e
```

Expected: 2 passing。

**Step 5:** 若卡在 session 创建，按顺序排查（每次只改一个变量）：

1. 确认二进制真的存在：`ls -la src-tauri/target/debug/tauri-app`
2. 手动起应用，看 4445 端口是否被监听：`./src-tauri/target/debug/tauri-app &` 然后 `curl http://127.0.0.1:4445/status`（应返回 WebDriver 的能力描述）
3. 报 `Tauri plugin not available` → 需补 `tauri-plugin-wdio`（提供 `browser.tauri.execute` 的那个）并在前端 `import '@wdio/tauri-plugin'`
4. 报窗口找不到 → 检查 `src-tauri/capabilities/default.json` 的 `"windows": ["main"]` 是否匹配实际窗口 label

**Step 6:** 提交

```bash
git add e2e package.json package-lock.json
git commit -m "test(e2e): webdriverio smoke test via embedded driver"
```

---

#### Task 9: 补真实业务场景 e2e

**Files:** Create `e2e/specs/timing.e2e.ts`, `e2e/specs/hotkeys.e2e.ts`, `e2e/specs/quit.e2e.ts`

**写测试前必读的 3 个坑（都已读源码确认）**

1. **控制条 3 秒后淡出**：`src/routes/+page.svelte:30` 里 `setTimeout(() => (controlsVisible = false), 3000)`。淡出只改 `opacity-0`，元素仍在 DOM 里、仍可点击，但视觉断言会翻车。**每次操作前先 `await browser.$('main').moveTo()` 唤回**（`wake()` 绑定在 `onmousemove` / `onpointerdown`）。
2. **计时显示格式**：`formatClock` 是 `mm:ss` 且**向上取整**（`src/core/format.ts:4`）。所以刚进入一个 3:00 的环节时显示 `03:00`，推进 1 秒左右后可能还是 `03:00`。断言时用"小于之前的秒数"或直接断言按钮文案，比断言精确文本稳。
3. **开始/暂停共用同一个按钮**：`src/components/ControlBar.svelte:22` 按 `timer.status` 切文案。判断运行时状态最稳的方式是断言 `button=暂停` 是否存在。

**Step 1:** 写计时与切环节：

```ts
// e2e/specs/timing.e2e.ts
describe("计时核心", () => {
  async function wake() {
    await $("main").moveTo(); // 唤回淡出的控制条
  }

  it("空格开始后按钮变为「暂停」", async () => {
    await wake();
    await browser.keys(["Space"]);
    await expect($("button=暂停")).toBeDisplayed();
    await browser.keys(["Space"]); // 还原成暂停态，避免影响后续用例
  });

  it("按 Enter 进入下一环节", async () => {
    await wake();
    const before = await $(".tnum").getText();
    await browser.keys(["Enter"]);
    await browser.pause(300);
    const after = await $(".tnum").getText();
    expect(after).not.toBe(before);
  });

  it("重置把剩余时间还原", async () => {
    await wake();
    await browser.keys(["Space"]);
    await browser.pause(1500);
    await browser.keys(["Space"]);
    await browser.keys(["r"]);
    await browser.pause(300);
    await expect($("button=开始")).toBeDisplayed();
  });
});
```

**Step 2:** 写那个最值钱的守卫——**输入态屏蔽单键**（这个坑在 `src/core/hotkeys.ts` 顶部注释里被明确点名，是现场最容易踩的）：

```ts
// e2e/specs/hotkeys.e2e.ts
describe("改辩题时的快捷键守卫", () => {
  it("在辩题输入框里按空格不会误启停计时", async () => {
    await $("main").moveTo(); // 唤回控制条
    await browser.keys(["t"]); // 打开辩题编辑（T 快捷键）
    const input = $("input");
    await expect(input).toBeFocused();

    await browser.keys([" "]); // 这是关键一击
    await browser.pause(300);

    // 计时不该被启动：控制条上仍是「开始」而不是「暂停」
    await $("main").moveTo();
    await expect($("button=开始")).toBeDisplayed();

    // 收尾：Esc 退出编辑
    await browser.keys(["Escape"]);
    await browser.pause(200);
  });
});
```

**Step 3:** 写退出入口（对应已验收的 feedback：全屏应用必须有显式退出 + 二次确认。`src/components/QuitConfirm.svelte` 的 dialog 有 `role="dialog"` + `aria-label="退出确认"`，默认焦点在取消上）：

```ts
// e2e/specs/quit.e2e.ts
describe("退出入口", () => {
  it("点退出弹出二次确认，取消后应用仍在", async () => {
    await $("main").moveTo();
    await $("button=退出").click();

    const dialog = $('[role="dialog"][aria-label="退出确认"]');
    await expect(dialog).toBeDisplayed();
    await expect(dialog).toHaveText(expect.stringContaining("比赛进行中退出会中断计时"));

    await dialog.$("button=取消").click();
    await expect(dialog).not.toBeDisplayed();
    await expect($("main")).toBeDisplayed(); // 应用没被关掉
  });
});
```

> 注意：dialog 打开时页面上会有**两个**「退出」按钮（控制条一个、弹窗一个），所以选择器必须用 `dialog.$('button=退出')` 这种作用域写法，不能用全局 `$('button=退出')`。

**Step 4:** 跑：

```bash
xvfb-run -a npm run test:e2e
```

Expected: 全部通过。

**Step 5:** 提交

```bash
git add e2e/specs
git commit -m "test(e2e): timing, hotkey guard and quit confirmation flows"
```

---

### M3 · CI

---

#### Task 10: Linux 快速道 `quality.yml`

**Files:** Create `.github/workflows/quality.yml`

**Step 1:** 写 workflow。让 `npm run build` 排在 Rust 步骤之前，使产物状态与 `tauri build` 一致（**不是** panic 防护——见 §3.1）：

```yaml
name: quality

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: 格式 · 类型 · 单测
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: 安装前端依赖
        run: npm ci

      # 先出前端产物。实测确认：clippy / cargo test 并不要求 build/ 存在（见 §3.1），
      # 保留这一步是为了让 workflow 与 tauri build 的产物状态一致，不是因为会 panic。
      - name: 前端构建
        run: npm run build

      - name: 代码格式检查（Prettier）
        run: npm run format:check

      - name: 类型检查（svelte-check）
        run: npm run check

      - name: 单元测试（Vitest）
        run: npm test

      - name: 安装 Rust 工具链
        uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: "./src-tauri -> target"

      - name: 代码格式检查（rustfmt）
        run: npm run fmt:rust

      - name: 代码规范检查（clippy）
        run: npm run lint:rust

      - name: 单元测试（Rust）
        run: npm run test:rust
```

> `Swatinem/rust-cache` 的 `workspaces` **必须显式写**：默认是 `. -> target`，而本项目根目录不是 Cargo workspace 根，写错就完全缓存不到（[rust-cache README](https://github.com/Swatinem/rust-cache)）。Tauri 官方示例也是这个写法。
> 工具链由根目录 `rust-toolchain.toml` 固定（Task 1），CI 与本地一致。

**Step 2:** 提交并推一个测试分支，确认 workflow 变绿。

```bash
git add .github/workflows/quality.yml
git commit -m "ci: quality lane with prettier, svelte-check, vitest, rustfmt and clippy"
```

**Step 3:** 故意制造一次失败验证闸门有效：把 `src/core/format.ts` 里某个分号删掉 → 推上去，`format:check` 必须红。然后还原。

---

#### Task 11: 平台矩阵道 `platform.yml`

**Files:** Create `.github/workflows/platform.yml`

**Step 1:** 写 workflow：

```yaml
name: platform

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: platform-${{ github.ref }}
  cancel-in-progress: true

jobs:
  platform:
    name: ${{ matrix.os }} · ${{ matrix.role }}
    runs-on: ${{ matrix.os }}
    continue-on-error: ${{ matrix.probe }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # 阻塞行：PR 必跑
          - { os: ubuntu-24.04, role: 编译+e2e, e2e: true, probe: false }
          - { os: windows-2022, role: 编译+e2e, e2e: true, probe: false }
          - { os: macos-15, role: 编译+e2e, e2e: true, probe: false }
          - { os: windows-2025, role: 编译, e2e: false, probe: false }
          # 探测行：只在 main / 手动触发时跑，且不阻塞
          - { os: windows-2025, role: 探测-e2e, e2e: true, probe: true, main_only: true }
          - { os: windows-11-arm, role: 探测-编译+e2e, e2e: true, probe: true, main_only: true }

    steps:
      - name: 探测行仅在 main 或手动触发时运行
        if: ${{ !matrix.main_only || github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/main' }}
        run: echo "本次运行此探测行"

      - uses: actions/checkout@v7
        if: ${{ !matrix.main_only || github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/main' }}

      - name: Linux 系统依赖
        if: matrix.os == 'ubuntu-24.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libxdo-dev \
            libssl-dev \
            build-essential curl wget file patchelf \
            webkit2gtk-driver xvfb

      - uses: actions/setup-node@v7
        if: ${{ !matrix.main_only || github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/main' }}
        with:
          node-version: 22
          cache: npm

      - uses: dtolnay/rust-toolchain@stable
        if: ${{ !matrix.main_only || github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/main' }}

      - uses: Swatinem/rust-cache@v2
        if: ${{ !matrix.main_only || github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/main' }}
        with:
          workspaces: "./src-tauri -> target"

      - name: 安装依赖
        if: ${{ !matrix.main_only || github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/main' }}
        run: npm ci

      - name: 编译检查（真打包）
        if: ${{ !matrix.main_only || github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/main' }}
        run: npm run tauri build

      - name: 构建 e2e 目标（debug，不打包）
        if: matrix.e2e && (!matrix.main_only || github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/main')
        run: npm run test:e2e:build

      - name: e2e（Linux，虚拟显示）
        if: matrix.e2e && matrix.os == 'ubuntu-24.04'
        run: xvfb-run -a npm run test:e2e

      - name: e2e（Windows / macOS）
        if: matrix.e2e && matrix.os != 'ubuntu-24.04'
        run: npm run test:e2e

      - name: 上传失败截图
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-artifacts-${{ matrix.os }}-${{ matrix.role }}
          path: e2e/artifacts/
          if-no-files-found: ignore
```

> **条件写法说明**：GitHub Actions 没有原生的"跳过整个 job"矩阵字段，所以用 `if:` 逐 step 控制。这样写的副作用是跳过时会留下一串绿色但空跑的 step。可读性更好的替代方案是把探测行拆成**独立的 job**（`platform-probe`），用同一个 workflow 文件里的第二个 `jobs:` 键。若 Step 2 发现这套 `if` 表达式太啰嗦，就重构成两个 job——功能等价，可读性明显更好。

**Step 2: 探测行的实际结果要回收**

- 先只推 main 跑一次，看 `windows-2025` 与 `windows-11-arm` 两行的探测结果。
- `windows-2025` 的 e2e **跑绿**：把它提升为阻塞行（`e2e: true, probe: false`，并在上面的阻塞组里加一行），同时更新 §2 的证据段落，注明"#14738 只影响经 CDP 的 provider，embedded provider 不受影响——已在 N 次运行中验证"。
- `windows-11-arm` 的编译或 e2e **红了**：如实记录到 §7，保持非阻塞，并在 README 里写明"真 Windows 11 仅 arm64 且尚未跑通"。**不要**为了让 CI 变绿而把它删掉——它的存在本身就是覆盖证据。

**Step 3:** 提交

```bash
git add .github/workflows/platform.yml
git commit -m "ci: cross-platform compile and desktop e2e matrix"
```

---

#### Task 12: 配置 required status checks

**Files:** 无（仓库设置）

**Step 1:** 在 GitHub 仓库 Settings → Branches 里，给 `main` 加保护规则，required checks 只勾**阻塞项**：

- `quality / 格式 · 类型 · 单测`
- `platform / ubuntu-24.04 · 编译+e2e`
- `platform / windows-2022 · 编译+e2e`
- `platform / macos-15 · 编译+e2e`
- `platform / windows-2025 · 编译`

**不要**把探测行勾成 required——`continue-on-error` 的 job 状态不稳定，勾了会把 PR 卡成永久 pending。

**Step 2:** 在 PR 上验证：故意让 `format:check` 失败，确认 PR 被挡住；还原后确认可以合并。

---

### M4 · 收尾

---

#### Task 13: 补 README

**Files:** Modify `README.md`

在"从源码运行 / 打包"一节后面加：

````markdown
### 测试

```bash
npm run verify        # 一次性跑：构建 + 格式 + 类型 + 单测 + Rust 检查
npm run format        # 自动修格式
npm test              # 前端单测（Vitest）
npm run test:e2e      # 桌面 e2e（WebdriverIO，裸机）
xvfb-run -a npm run test:e2e   # 无显示器的 Linux
```

> 实测确认：`cargo check` / `clippy` / `test` **不要求** `build/` 存在（原因见 §3.1）。
> 只有 `npm run tauri build` 会要求前端产物，而它自己会先跑 `beforeBuildCommand`。
> 想一条命令跑完全部闸门就用 `npm run verify`。

### 平台覆盖的已知缺口

CI 在以下平台跑 e2e 与编译检查：

| CI runner        | 对应的真实环境                     |
| ---------------- | ---------------------------------- |
| `ubuntu-24.04`   | Ubuntu 24.04                       |
| `windows-2022`   | Windows 10 世代内核（Server 2022） |
| `windows-2025`   | Windows 11 世代内核（Server 2025） |
| `macos-15`       | macOS 15 (arm64)                   |
| `windows-11-arm` | 真 Windows 11（arm64，实验性）     |

**没有**覆盖的：Windows 10 / 11 **消费版**桌面。GitHub 托管 runner 不提供消费者版本镜像，
因此以下项目仍需人工在真机上验收——消费级 WebView2 版本差异、DPI 缩放、
Defender / SmartScreen 拦截、非管理员权限运行。
````

**Step 2:** 提交

```bash
git add README.md
git commit -m "docs: document test commands and platform coverage gaps"
```

---

#### Task 14: 端到端验证一次

**Files:** 无

**Step 1:** 开一个真实 PR，确认：

- 两条 workflow 都触发
- 五个阻塞 job 全绿
- PR 页面能看到 e2e 的 spec 输出

**Step 2:** 故意改坏一处（如 `src/core/format.ts` 去掉一个分号），确认 `quality` 变红且 PR 被挡。

**Step 3:** 还原，确认变绿。

**Step 4:** 把本次新增的 workflow 名称与 runner 映射写进 `docs/plans/2026-09-22-00-总览与对比.md` 的收尾章节（保持项目文档单一入口的习惯）。

---

## 6. 风险登记

| 风险                                                                      | 影响                                                      | 落点                                                                                                |
| ------------------------------------------------------------------------- | --------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| ~~`build/` 缺失导致 Rust 闸门 panic~~ **已证伪（2026-09-22）**            | ~~高~~ 无：`check`/`clippy`/`test` 实测缺 `build/` 也通过 | §3.1 + Task 6 实测记录；仅 `tauri build`（启用 custom-protocol）会触发，且它自带 beforeBuildCommand |
| `devUrl` 被移除后上述结论失效，`cargo check` 会重新在缺 `build/` 时 panic | 低（但会突然炸）                                          | §3.1 结尾的警告；改动 `tauri.conf.json` 时留意                                                      |
| release 构建因 capability 引用已移除插件的权限而失败                      | 高（会挡住发布）                                          | Task 7 Step 4/5 + 兜底方案 B                                                                        |
| release 包意外带上可远程驱动的 WebDriver 服务                             | **严重（安全）**                                          | Task 7 Step 5 端口探测                                                                              |
| 控制条 3s 淡出让 e2e 视觉断言翻车                                         | 中                                                        | Task 9 的 `wake()` helper                                                                           |
| `npm create wdio` / 官方 Tauri 文档教的是已失效的 `tauri-driver` 路线     | 中（照抄即失败）                                          | 全部改用 embedded provider，§2 已记录                                                               |
| macOS 上 embedded provider 是否需要签名 / TCC 授权**未能确认**            | 中                                                        | Task 8 先在 Linux 跑通；macOS 首次运行若失败，按平台单独排查，并把结论写回 §7                       |
| `windows-11-arm` 的 `@wdio/tauri-service` 支持未知                        | 低（已设非阻塞）                                          | Task 11 Step 2 探测                                                                                 |
| Prettier 一次性重排全仓库                                                 | 低（但污染 blame）                                        | Task 2 Step 5 的 >5 文件则单独 commit                                                               |
| 4 个平台 × 全量 `tauri build` 的 CI 耗时                                  | 中（成本）                                                | §6.3 缩量开关                                                                                       |
| 全屏下 WebdriverIO 发按键是否有平台差异**未能确认**                       | 中                                                        | Task 9 实测；必要时退化为点击而非按键                                                               |

### 6.3 CI 成本与缩量开关

Tauri 官方文档提到未缓存的冷启动 `create-tauri-app` 项目构建约 **1 小时量级**（[Tauri 流水线文档](https://tauri.app/distribute/pipelines/github/)）；x64 原生 runner 的具体耗时官方没给数字，**未能确认**，实际以首次跑通的数据为准。

用户已明确要 PR 上跑三平台全量 e2e。若日后觉得 PR 反馈太慢，按代价从低到高依次缩：

1. 把 `windows-2025` 编译行挪到 main-only（省 1 次 Windows 全量打包）
2. 把 e2e 的 `tauri build` 从全量打包改成 `npm run test:e2e:build`（`--no-bundle`，跳过 bundler，省最多时间）
3. `macos-15` 的 e2e 改成 main-only
4. 如上仍不够，退到"PR 只跑 Linux e2e"

> 第 2 条其实可以**立即**用在阻塞行上：`--no-bundle` 已经足够证明"能编译"，而打包（deb/msi/dmg）留到 release 流水线再做。当前计划让阻塞行跑全量 `tauri build` 是为了顺带把打包也验证掉；若耗时不可接受，这是第一个该动的地方。

---

## 7. 未决 / 需实测确认（不要在未验证前当成结论）

| 项                                                                      | 为什么未决                                                                                      | 怎么确认                                                |
| ----------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| `[target.'cfg(debug_assertions)'.dependencies]` 是否让 release 构建失败 | WebdriverIO 文档推荐此写法，但没说明 capability 权限在 release 下如何解析；Tauri 官方文档未覆盖 | Task 7 Step 4 实测；失败则用兜底方案 B                  |
| macOS 上 embedded provider 是否需要签名 / TCC 授权                      | WebdriverIO 文档**完全没提**，也没找到 gatekeeper 卡住无头 CI 的报告。"文档没提"不等于"不需要"  | Task 11 首次 macOS e2e 运行结果                         |
| `@wdio/tauri-service` 是否支持 win32-arm64                              | 官方平台支持表只区分 Windows/Linux/macOS，未区分架构                                            | Task 11 探测行                                          |
| `browser.keys()` 在 Tauri 全屏窗口下各平台行为是否一致                  | Tauri / WebdriverIO / desktop-mobile 文档与 issue 里均无相关说明                                | Task 9 实测；不行就改用点击 `button=开始` 等元素操作    |
| `macos-26` 的 embedded provider 回归是否已在 1.4.0 修复                 | 修复 PR webdriverio/desktop-mobile#549 已合并，但不确定是否进了 1.4.0                           | 需要时加探测行，绿了再把 `macos-15` 换成 `macos-latest` |
| 是否需要 `withGlobalTauri: true`                                        | 只在插件排障文档里作为检查项出现；开了会在 release 暴露 `window.__TAURI__`                      | Task 8 Step 5 排查；仅当必须才开，并记录安全影响        |
| 是否需要 `tauri-plugin-wdio`（提供 `browser.tauri.execute` 的那个）     | 有文档说 `@wdio/tauri-service` 会强制检查它，但主文档的最小示例又没装                           | Task 8 Step 5 第 3 条                                   |

### 后续可选扩展（本次不做）

- **自建 runner 挂真 Windows 10 机器**：唯一能字面覆盖真 Win10 的方案。做法是**单独一个 workflow**（不要并入 `platform.yml`），且**绝不设为 required check**——官方没有非阻塞 job 机制，runner 离线时 job 会排满 24 小时才被取消（[limits](https://docs.github.com/en/actions/reference/limits)），设成 required 会直接卡死所有 PR。
- **`dockur/windows` 探测**：想验证真 Win10/11 内核行为时的实验手段。前置条件是宿主有 `/dev/kvm`（标准 runner 上官方不支持、无 SLA），且需要自行确认 Windows 客户端虚拟化的许可。仅当上一条（自建 runner）不可行、又确实需要真 Win10 覆盖时才考虑。

---

## 8. 执行顺序总览

```
M1 基础设施      Task 1  rust-toolchain.toml
                Task 2  Prettier（先量后改）
                Task 3  npm scripts
                Task 4  单测基线确认
                Task 5  登记 Rust 零测试
                Task 6  实测 frontendDist panic 触发条件（结论已推翻原假设）
M2 e2e 地基      Task 7  加 embedded 插件 + release 安全验证   ← 最高风险
                Task 8  最小 smoke 本地跑通
                Task 9  真实业务场景 spec
M3 CI           Task 10 quality.yml
                Task 11 platform.yml（含探测行）
                Task 12 required checks
M4 收尾         Task 13 README
                Task 14 端到端验证 + 更新总览文档
```

Task 7 是唯一有可能推翻方案的地方（release 构建 + 安全验证）。建议**先把 Task 7 做完再动 M3**，不要在 e2e 地基未验证时就把 workflow 写完。
