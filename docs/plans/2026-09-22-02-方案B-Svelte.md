# 方案 B：Svelte 5 + runes（推荐，最短路径 / 最小体积）

> 公共基线见 `2026-09-22-00-总览与对比.md`（数据模型、引擎、快捷键、内置赛制、视觉方向均共用）。
> 本文只写 B 方案特有的技术栈、目录结构、实现要点、里程碑与风险。

---

## 1. 技术栈（版本均为 2026-09-22 实测 `npm view` 结果）

| 层 | 选型 | 版本 | 理由 |
|---|---|---|---|
| 桌面壳 | Tauri | **2.11.6** | 当前稳定版（crates.io 已核实） |
| 构建 | Vite | **8.3.0** | 由官方脚手架决定实际版本 |
| UI 框架 | Svelte | **5.57.1** | runes（`$state`/`$derived`/`$effect`）已稳定 |
| 语言 | TypeScript | 最新 | `svelte-check` 做类型检查 |
| 样式 | Tailwind CSS | **4.3.3** | CSS-first 配置 |
| 状态 | Svelte runes（`.svelte.ts` 里导出 `$state`） | — | 不需要额外状态库 |
| 动画 | **Svelte 内置** `transition` / `spring` / `tweened` | 内置 | 零额外依赖，这是 B 相对 A 的最大优势 |
| 图标 | Phosphor Icons（`@phosphor-icons/svelte`）或 Heroicons | 最新 | SVG，勿用 emoji |
| 测试 | Vitest | 最新 | 主测 `core/`（纯 TS，与框架无关） |
| 包管理 | **npm** | 9.2.0 | 本机 pnpm 不可用 |

---

## 2. 目录结构

```
DebateTImer/
├─ src/
│  ├─ core/                    # 纯 TS，零框架 / 零 Tauri 依赖（与 A/C 完全一致）
│  │  ├─ models.ts  engine.ts  clock.ts
│  │  ├─ presets.ts storage.ts audio.ts hotkeys.ts
│  ├─ lib/
│  │  ├─ timer.svelte.ts       # runes 状态：把 engine 包成响应式
│  │  └─ config.svelte.ts      # 模板、辩题、偏好（$state + localStorage 同步）
│  ├─ components/
│  │  ├─ TimerDisplay.svelte
│  │  ├─ StageBar.svelte
│  │  ├─ TopicHeader.svelte
│  │  ├─ ControlBar.svelte
│  │  ├─ PromptOverlay.svelte
│  │  ├─ FreeDebatePanel.svelte
│  │  ├─ SettingsPanel.svelte
│  │  └─ FlowEditor.svelte
│  ├─ App.svelte
│  └─ main.ts
├─ src-tauri/
└─ docs/plans/
```

---

## 3. 关键实现要点

### 3.1 高频 tick：Svelte 的细粒度响应式天然适配

Svelte 5 编译期就知道哪个文本节点依赖哪个变量，更新只改那一个节点，**没有 VDOM diff**。所以可以直接把剩余时间放进 `$state`，不需要 A 方案那种"绕过框架直写 DOM"的绕路。

```ts
// lib/timer.svelte.ts
import { TimerEngine } from '../core/engine';

export const engine = new TimerEngine();

class TimerView {
  remainMs = $state(0);
  status   = $state<'idle'|'running'|'paused'|'finished'>('idle');
  stageIndex = $state(0);

  constructor() {
    engine.onTick((s) => { this.remainMs = s.remainMs; this.status = s.status; });
  }
}
export const timer = new TimerView();
```

```svelte
<!-- TimerDisplay.svelte -->
<div class="tabular-nums font-semibold" style="font-size: clamp(4rem, 22vh, 24rem)">
  {format(timer.remainMs)}
</div>
```

### 3.2 动画几乎免费

```svelte
<!-- PromptOverlay.svelte：提示横幅 -->
{#if prompt}
  <div in:fly={{ y: -24, duration: 260, easing: cubicOut }}
       out:fade={{ duration: 180 }}
       class="...">{prompt.label}</div>
{/if}
```

环节切换、数字颜色过渡（正常 → 30s 琥珀 → 到点红）、控制条淡出，全部用内置 transition/`tweened` 完成，不需要装 `motion`。

### 3.3 engine / 提示点 / 时钟校准

与总览 §3.3 完全一致（`core/` 三方案共用，零框架依赖）：
- 提示点预算成绝对时间戳数组，tick 做"跨过检测"，休眠醒来一次性补触发；
- `performance.now()` 为主、`Date.now()` 为裁判，偏差 > 1s 判定休眠并按墙钟重算 deadline。

### 3.4 Tauri 侧

与 A 一致：单窗口全屏配置、按需 capabilities、不引入全局热键（窗口内 `keydown` 足够）。
Svelte 需确保构建产物是**纯静态 SPA**（`ssr = false`，禁用 prerender），否则 Tauri 加载会失败：

```js
// svelte.config.js
export default { compilerOptions: {}, kit: { adapter: adapterStatic? } }
```
> 注：若用 **SvelteKit**，必须配 `adapter-static` + `ssr: false`；若直接用 **Vite + Svelte（非 Kit）**，则天然是 SPA，配置更少——**本项目单屏、无路由，推荐直接用 `vite` + `svelte` 插件，不引入 SvelteKit**。

### 3.5 字体与离线

字体 woff2 放 `src/assets/fonts/`，CSS `@font-face` 本地引用，**不引 Google Fonts CDN**（教室可能没网）。

---

## 4. UI 布局（单屏全屏，浅色简洁）

与总览 §5 / 方案 A §4 相同：

```
┌──────────────────────────────────────────────────────────┐
│ 辩题：人工智能是否会加剧教育不平等          正方 │ 反方   │  ← 可点击内联编辑
│                      03:00                               │  ← 巨型数字（tabular-nums，22vh）
│                 正方一辩 · 立论                          │
│              ▁▁▁▁▁▁▁▁▁▁▁▁░░░░░░░░                        │
│        [反方 04:12]        [正方 03:58]                  │  ← 仅自由辩论出现
│  ⏵开始   ⏭下一环节   ⏮上一   ↺重置   +10s   −10s   ⚙设置 │  ← 静止 3s 淡出
└──────────────────────────────────────────────────────────┘
```

---

## 5. 里程碑（B 方案）

| 阶段 | 内容 | 完成判据 |
|---|---|---|
| M0 | `npm create tauri-app@latest`（选 Svelte + TS）→ 空全屏窗口 | `npm run tauri dev` 起窗 |
| M1 | `core/` 引擎 + Vitest 单测 | 单测：暂停恢复不丢时、提示点不漏、休眠校准、±加时 |
| M2 | `TimerDisplay` + `ControlBar` + `TopicHeader` | 单环节计时可跑，辩题可改 |
| M3 | 内置 5 套模板 + 环节流转 + `FreeDebatePanel` + 提示（声/视） | 双边计时正确；奥瑞冈三级铃正确 |
| M4 | `FlowEditor` + 导入导出 JSON | 新建赛制，重启后仍在 |
| M5 | 快捷键 + 设置面板 + 动效打磨 + 三平台打包 | 三平台有安装包；输入框聚焦不误触发 |

---

## 6. B 方案特有风险

| 风险 | 对策 |
|---|---|
| Svelte 5 runes 心智模型与 React 不同（`$state` 是编译期魔法） | `core/` 保持纯 TS 不碰 runes，只有 `lib/*.svelte.ts` 薄封装用 |
| 生态小于 React，第三方组件少 | 本项目组件自制，无对外依赖需求 |
| SvelteKit vs 纯 Vite+Svelte 选错会增加配置 | **选纯 `vite` + `svelte` 插件**，无路由需求，避免 SSR/prerender 坑 |
| 未来若要招人接手，会 Svelte 的人少于 React | 若这是长期多人项目，改选方案 A |

---

## 7. 选 B 的理由 / 不选 B 的理由

**选（推荐）**：本项目规模小但要求"UI 精美 + 动效顺滑"，Svelte 内置动画 + 细粒度更新是最短路径；产物最小（~10-15KB 运行时），老旧多媒体电脑启动最快；代码量比 A 少约 30-40%。
**不选**：你需要 React 生态，或这是长期多人协作项目。
